# Reference only — Railway does NOT read this file. Paste real values into
# Railway's Variables tab directly. Never put real secrets in this file or
# commit them to git.

# Shared secret for /admin endpoints.
API_KEY=

# Xero — Custom Connection app "EP-Agent", for Everest Plunge's OWN Xero
# organisation. Do not reuse a Kiwiseal Xero app. This is a Custom
# Connection (client_credentials grant), not the redirect-based OAuth the
# older Kiwiseal Xero agents use — no redirect URI, no refresh token, no
# browser flow needed. Just paste the Client ID/Secret once the Connection
# invite has been approved inside Xero's own UI (see README's setup
# checklist).
XERO_CLIENT_ID=
XERO_CLIENT_SECRET=

# Leave blank — auto-discovered on first Xero API call and cached in
# memory. Only set this if you need to override auto-discovery for some
# reason.
XERO_TENANT_ID=

# Pipely (GoHighLevel) — Everest Plunge's OWN location, not Kiwiseal's.
# Find the location ID in Pipely's Settings > Business Info, and generate
# an API key under Settings > API Keys (or Private Integrations, depending
# on your Pipely plan).
PIPELY_API_KEY=
PIPELY_LOCATION_ID=
PIPELY_BASE_URL=https://services.leadconnectorhq.com

# How far back to look for "won" Pipely deals on each check. Deals won
# earlier than this are assumed already reconciled (or already dealt with)
# and are silently ignored, not flagged.
RECONCILE_LOOKBACK_DAYS=30

# How often to re-run the reconciliation check, in minutes.
CHECK_INTERVAL_MINUTES=60

# Dollar tolerance when comparing a Pipely deal's value against a Xero
# invoice total — small differences (rounding, a discount applied in Xero
# but not reflected in the Pipely deal value) shouldn't count as a mismatch.
VALUE_TOLERANCE=1

# Where the current mismatch snapshot is persisted (survives restarts if a
# Railway volume is mounted here).
MISMATCH_FILE=/data/pipely-xero-mismatches.json

# --- Deposit invoicing (added 2026-08-24) ---
# Shared secret for the deposit-trigger webhook, sent by the GoHighLevel
# Workflow action either as ?token=... on the URL, or as an
# x-webhook-secret header, whichever your workflow setup supports. This is
# separate from API_KEY above — GHL's workflow webhook action likely can't
# send a custom x-api-key header the way Retool/Business Brain can.
PIPELY_WEBHOOK_SECRET=

# Fraction of the deal value invoiced as the booking deposit.
DEPOSIT_PERCENTAGE=0.5

# Reuses the same chart-of-accounts values as everest-plunge-shopify-xero-
# agent — fill in the real codes from your Xero org, don't guess.
XERO_SALES_ACCOUNT_CODE=
XERO_TAX_TYPE=

# Where flagged deposit-invoice failures are persisted.
DEPOSIT_FAILED_LOG_FILE=/data/deposit-invoice-failures.json

# --- Stock sheet link (added 2026-08-31) — optional. Same stock sheet
# agent the Shopify agent talks to. Every deposit-invoiced Pipely deal gets
# flagged for manual SKU assignment (GET /admin/needs-sku-assignment,
# POST /admin/assign-sku) until a Pipely product field exists to resolve
# it automatically. Leave blank to skip this entirely.
STOCK_SHEET_AGENT_URL=
STOCK_SHEET_AGENT_API_KEY=
NEEDS_SKU_FILE=/data/pipely-needs-sku.json

PORT=3008
